# Keep formatting stable independently of the Rust language edition.
# The workspace uses Rust 2024 semantics, while the existing source layout
# follows the pre-2024 rustfmt style.
style_edition = "2021"
