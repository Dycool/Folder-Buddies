// Shared resource id for the embedded WinFsp installer. Included by both the
// Windows resource script (winfsp.rc.in) and the bootstrap code so the two
// never drift apart.
#pragma once
#define IDR_WINFSP_MSI 1001
