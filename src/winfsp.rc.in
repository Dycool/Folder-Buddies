// Generated by CMake (configure_file). Embeds the official WinFsp MSI into the
// .exe so the runtime can install it silently on first mount. Only used on
// Windows builds where the MSI was made available at configure time.
#include "winfsp_resource.h"
IDR_WINFSP_MSI RCDATA "@WINFSP_MSI_PATH@"
